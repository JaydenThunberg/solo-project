// this will be the individual items from the map of favorites
import React, { Component } from 'react';

class FavoriteItem extends Component {
  // Renders the entire FavoriteItem on the DOM
  render() {
    return (
      <div className="FavoriteItem">
        <p>FavoriteItem</p>
      </div>
    );//end return
  }//end render
}// end class

export default FavoriteItem;